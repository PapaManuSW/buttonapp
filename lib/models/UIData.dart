class UIData {
  int streak;
  double percentageRemainingTime;
  String remainingTimeText;
  int remainingTimeSeconds;

  UIData(this.streak, this.percentageRemainingTime, this.remainingTimeText, this.remainingTimeSeconds);
}
